#include<iostream>
#include<cmath>
int main()
{
    double x=5.99;
    double y=4;
    double z;
    //z=std::max(x,y);
    //z=std::min(x,y);
    //z=std::pow(2,3);
   // z=std::sqrt(100);
    //z=abs(-3);
    //z=round(x);
     // rounds up z=ceil(x);
     // rounds down z=floor(x);

std::cout<<z<<'\n';

    return 0;
}